linters:
  enable:
    - unused
    - ineffassign
    - staticcheck
    - govet
    - revive
    - gosimple
    - errcheck

linters-settings:
  revive:
    rules:
      - name: exported
        arguments:
          - checkPrivateReceivers: false

run:
  timeout: 5m
